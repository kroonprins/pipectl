* diff
* no api call if no diff
* support
  * --selector / -l
  * --timeout
  * --validate
  * multiple -f
* transformations:
  * sensible defaults
  * add rank automatically
  * variable groups by name
  * task group by name